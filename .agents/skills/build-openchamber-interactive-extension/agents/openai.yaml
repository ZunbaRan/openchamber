interface:
  display_name: "Build OpenChamber Interactive Extension"
  short_description: "Build signed OCIX enterprise UI and Agent runtime"
  default_prompt: "Use $build-openchamber-interactive-extension to choose Declarative or Trusted Native Host UI Kit, build the business Gateway and Agent runtime, then validate, sign, install, and test the complete OCIX conversation path."
