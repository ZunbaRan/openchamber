# OpenCode instructions fragment — Generative Widget

Append the contents of `packages/ui/src/lib/generative-widget/guidelines.ts`
(`GENERATIVE_WIDGET_WIRE_FORMAT` then `GENERATIVE_WIDGET_SYSTEM_PROMPT`) to the
agent system/instructions path for sessions where generative widgets are enabled.

Do not register a tool. Widgets are pure assistant text fences rendered by OpenChamber UI.
